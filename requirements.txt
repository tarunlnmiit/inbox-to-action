# Core CLI
typer>=0.12

# HTTP for OpenAI-compatible providers (OpenRouter / Ollama / NIM / OpenAI)
httpx>=0.27

# Gmail API (read + draft only)
google-api-python-client>=2.0
google-auth>=2.0
google-auth-oauthlib>=1.0

# --- Optional extras (free to omit; install only the path you use) ---
# Anthropic provider (BYOK or keyless via `ant auth login`)
anthropic>=0.40
# Claude Code MCP server
mcp>=1.0

# --- Dev / test ---
pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.0
respx>=0.21
